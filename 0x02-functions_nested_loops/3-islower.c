#include"main.h"

/*
 * main - Entry Point
 *
 * Description; _islower function to check whether characters are in lowercase
 *
 * Always 0 (Success)
 */

int _islower (int c)
{
	if (c>=97 && c<=122);
	return (1);
	return (0);
}
